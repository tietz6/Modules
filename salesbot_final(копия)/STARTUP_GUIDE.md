# 🚀 Руководство по запуску проекта

## Описание проекта

Telegram-бот **Tietz** для обучения продажников работе с персональными песнями и сопутствующими услугами.

**Архитектура:**
- Backend: FastAPI (модульная система)
- Telegram Bot: простой HTTP-клиент (без aiogram)
- ИИ-наставник: DeepSeek (персона Tietz)

## Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные зависимости:**
- fastapi >= 0.104.0
- uvicorn[standard] >= 0.24.0
- httpx >= 0.25.0
- requests >= 2.31.0
- python-dotenv >= 1.0.0

## Запуск Backend (FastAPI)

### Способ 1: Через main.py

```bash
python main.py
```

Backend запустится на `http://0.0.0.0:8080`

### Способ 2: Через uvicorn напрямую

```bash
uvicorn startup:app --host 0.0.0.0 --port 8080 --reload
```

### Проверка работы Backend

```bash
# Проверка health
curl http://localhost:8080/api/public/v1/health

# Список подключённых модулей
curl http://localhost:8080/api/public/v1/routes_summary
```

**Ожидаемый результат:** 5 модулей подключены:
- modules.master_path.v3.routes
- modules.objections.v3.routes
- modules.upsell.v3.routes
- modules.arena.v4.routes
- modules.sleeping_dragon.v1.routes

## Запуск Telegram бота

### Настройка переменных окружения

Создайте файл `.env`:

```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
BACKEND_URL=http://127.0.0.1:8080
```

Или установите переменные окружения:

```bash
export TELEGRAM_TOKEN="your_token_here"
export BACKEND_URL="http://127.0.0.1:8080"
```

### Запуск бота

```bash
python simple_telegram_bot.py
```

**Важно:** Сначала запустите Backend, затем бота!

## Структура модулей

### 🧭 Путь Мастера (master_path)
**Эндпоинт:** `/master_path/v3/start`  
**Описание:** Полный цикл продажи от приветствия до закрытия

### 🛡️⚔️ Щит и Меч (objections)
**Эндпоинт:** `/objections/v3/start`  
**Описание:** Тренировка работы с возражениями

### 🏆 Вкус Победы (upsell)
**Эндпоинт:** `/upsell/v3/start`  
**Описание:** Обучение мягким допродажам и акции 3+1

### 🐉 Спящий Дракон (sleeping_dragon)
**Эндпоинт:** `/sleeping_dragon/v1/start`  
**Описание:** Работа с неактивными клиентами (3 волны сообщений)

### 🎭 Арена (arena)
**Эндпоинт:** `/arena/v4/start`  
**Описание:** Свободная практика с AI-клиентами

## Использование бота

### Команды

- `/start` - Главное меню с кнопками
- `/modules` или `/menu` - Показать модули обучения
- `/train` - Быстрый старт (запускает Путь Мастера)

### Интерфейс кнопок

При запуске `/start` пользователь видит:

```
Я новичок          → Запуск Пути Мастера
Я уже с базой      → Главное меню модулей
```

В главном меню:

```
🧭 Путь Мастера
🛡️⚔️ Щит и Меч
🏆 Вкус Победы
🐉 Спящий Дракон
🎭 Арена
```

## Тестирование модулей

### Проверка через curl

```bash
# Проверка доступности модуля
curl -X POST http://localhost:8080/master_path/v3/start \
  -H "Content-Type: application/json" \
  -d '{"probe": true}'

# Запуск модуля для пользователя
curl -X POST http://localhost:8080/master_path/v3/start \
  -H "Content-Type: application/json" \
  -d '{"chat_id": 12345}'
```

### Проверка всех модулей

```bash
for module in master_path/v3 objections/v3 upsell/v3 arena/v4 sleeping_dragon/v1; do
  echo "Testing $module"
  curl -s -X POST "http://localhost:8080/$module/start" \
    -H "Content-Type: application/json" \
    -d '{"probe": true}' | python -m json.tool
done
```

## Устранение неполадок

### Ошибка: "No module named 'uvicorn'"

```bash
pip install uvicorn
```

### Ошибка: "TELEGRAM_TOKEN required"

Установите переменную окружения:
```bash
export TELEGRAM_TOKEN="your_token_here"
```

### Ошибка: порт 8080 занят

Измените порт в `main.py`:
```python
uvicorn.run("startup:app", host="0.0.0.0", port=8081, reload=False)
```

### Backend запустился, но модули не работают

Проверьте логи при запуске - должны быть видны импортированные модули.

## Архитектура файлов

```
salesbot_final(копия)/
├── main.py                 # Точка входа для backend
├── startup.py              # FastAPI app с автозагрузкой
├── router_autoload.py      # Регистрация всех роутов
├── simple_telegram_bot.py  # Telegram бот (HTTP API)
├── requirements.txt        # Python зависимости
│
├── modules/                # Модули обучения
│   ├── master_path/v3/
│   ├── objections/v3/
│   ├── upsell/v3/
│   ├── arena/v4/
│   ├── sleeping_dragon/v1/
│   ├── main_menu/v1/
│   ├── deepseek_persona/
│   └── tietz_prompts/
│
└── api/
    └── core/
        ├── registry.py     # Реестр модулей
        └── module_loader.py
```

## Что дальше?

1. **Настройте API ключи** для DeepSeek в переменных окружения
2. **Протестируйте каждый модуль** через Telegram
3. **Настройте базу данных** для сохранения прогресса пользователей
4. **Добавьте мониторинг** и логирование ошибок

---

**Версия:** v1-final  
**Дата:** 2025-01-21
